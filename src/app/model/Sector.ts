/**
 * Classe que representa um setor
 */
export class Sector {

    id: string;
    description: string;

    constructor(id: string, description: string) {
        this.id = id;
        this.description = description
    }
}